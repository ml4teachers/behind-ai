# behind-ai – Redesign-Plan

> **Zentraler Plan & Fortschritt über alle Redesign-Threads.**
> Jeder Thread liest hier rein und hakt Erledigtes ab (`- [ ]` → `- [x]`).
> Der „Master-Thread" koordiniert; die einzelnen Threads setzen je einen Block um.

---

## Leitprinzipien

- **Operatives Prinzip:** wenig lesen, viel ausprobieren. Vorbild ist die **Next-Token-Seite** (echtes Modell, sofort spielbar).
- **Zeitlos & flach:** weniger verschachtelte Cards/farbige Boxen. Pro Seite *ein* starkes Interaktiv-Element zuoberst, knappe Caption, optionales „Mehr dazu" zum Aufklappen.
- **Zweisprachig denken:** Texte beim Neuschreiben direkt in die i18n-Schicht (DE füllen, EN folgt in Thread 9).
- **Echt statt simuliert**, wo machbar.
- **Echtheit zeigen, nicht beteuern:** Da die Demos echt *sind*, muss die Copy das nicht wiederholt betonen („echtes Modell", „läuft wirklich", „tatsächlich") – solche Beteuerungen wirken defensiv und redundant; die Interaktion belegt es selbst. In Captions **generisch** bleiben („ein Embedding-Modell", „ein Sprachmodell") statt Backend-Produktnamen („gemini-embedding-2", „Gemini über Vertex AI") – die konkrete Modellwahl gehört in Code/Doku, nicht in die Lern-Caption. Keine Klammer-Einschübe. (Ausnahme: relatable Anker wie „derselbe Tokenizer, den auch ChatGPT nutzt" oder der Datensatz-Name auf der Daten-Seite – dienen dem Lernen, kein Backend-Flex.)
- **Positiv formulieren:** Nutzen benennen, nicht Defizite – **keine Negativbeschriebe** („statt langer Texte", „kein …"). Sagen, was die Seite *bietet*.

## Harte Constraints

- **Alle bestehenden URLs bleiben** (aufgrund bestehender externer Verweise).
- **Production läuft von `main`** (Vercel). `main` bleibt bis zum bewussten Merge **unangetastet** – gesamte Redesign-Arbeit auf Branch **`redesign`**.
- **Keine Secrets committen** (`.env.local`, `gcp-service-account.json` sind gitignored).

## Branch-Workflow (Solo-Dev)

- Branch **`redesign`**: alle Threads committen hierhin. `main` bleibt die lauffähige Production-Linie.
- **Lokale Commits sind nicht live.** Erst `git push` lädt hoch; erst ein **Merge in `main`** (bzw. Push von `main`) ändert Production.
- Wenn ein kohärenter Stand erreicht ist: `redesign` → `main` mergen (ein Schritt), Production aktualisiert sich einmalig. Jederzeit abbrechbar.

## Architektur-Konventionen (ab Thread 1 verbindlich)

- **Farben nur als semantische Tokens:** `bg-background`, `text-foreground`, `text-muted-foreground`, `bg-card`, `border`, `bg-primary`, … – **keine** hartcodierten Farben (`bg-violet-50`, `text-gray-600` …). Nur so funktioniert Dark-Mode automatisch.
- **Akzentfarbe** ist die einzige „bunte" Farbe = `--primary` / `--primary-foreground` / `--ring`. Im Header umschaltbar ([`accent-toggle.tsx`](src/components/accent-toggle.tsx)); [`accent-provider.tsx`](src/components/accent-provider.tsx) injiziert die CSS-Vars (Light **und** Dark getrennt) aus der Tailwind-Palette in [`accents.ts`](src/lib/accents.ts). **Default „Sky"** (globals.css hält denselben SSR-Default → kein Flackern). Wer `bg-primary`/`text-primary` nutzt, erbt die Akzentfarbe automatisch.
- **Texte über i18n:** `const t = useTranslations(); t('key')`. Strings in [`src/lib/i18n/messages.ts`](src/lib/i18n/messages.ts) (DE füllen; EN darf bis Thread 9 auf DE zurückfallen).
- **UI-State** (locale, accent, sidebar) in [`src/lib/store.ts`](src/lib/store.ts) (`useUIStore`, persistiert). `useMounted` gegen Hydration-Mismatch.
- **Shell:** [`src/components/layout/app-shell.tsx`](src/components/layout/app-shell.tsx), [`site-header.tsx`](src/components/layout/site-header.tsx). **Nav:** [`src/components/nav/navigation.tsx`](src/components/nav/navigation.tsx) + [`nav-items.ts`](src/components/nav/nav-items.ts).

---

## Roadmap

### Thread 1 – Fundament & Shell ✅ erledigt
- [x] Theme-Tokens (Light + Dark, freundlich/neutral, Google-Blau) in `globals.css`
- [x] Dark-Mode via `next-themes` + Toggle
- [x] Ausblendbare Sidebar + Header (`app-shell`, `site-header`)
- [x] Navigation flach/neu; Zwei-Pfade-Struktur erhalten
- [x] Titel **„Behind AI"** (Header + Metadata)
- [x] Maskottchen (`GuideCharacter`) komplett entfernt
- [x] `/daten` → `/data` (Ordner umbenannt + 308-Redirect)
- [x] i18n-Gerüst (`messages`, `useTranslations`, `useUIStore`) – URLs unverändert
- [x] Build grün, Shell visuell & funktional verifiziert (Dark-Mode, Sprach-Toggle, Collapse persistieren)

### Thread 2 – Startseite & Informationsarchitektur ✅ erledigt
- [x] Home neu: „ausprobieren statt lesen" – knapper Hero + **echte Next-Token-Mini-Demo** zuoberst (Token-Klick hängt an & rechnet neu), Textwände raus
- [x] Sektions-Labels & Reihenfolge final: Lern-Bogen **Tokenisierung → Embeddings → Next-Token → Daten → Training → Finetuning → RLHF → CoT → RAG** (Embeddings nach vorn). Begründung in `nav-items.ts`
- [x] Home auf semantische Tokens + neue Copy (Light/Dark im Preview verifiziert)
- [x] Home-Texte nach `messages.ts` (DE **und** EN gefüllt, `home.*`)
- [x] Reste alter Copy bereinigt: alte Home-Copy ersetzt; kein „Behind ChatGPT"-Branding mehr im Code (nur legitime ChatGPT-Produktbeispiele)
- [x] **Akzentfarben-Picker** im Header (16 Tailwind-Farben mit Swatches, persistiert, Light+Dark). **Neuer Default „Sky"** statt Blau. Siehe Architektur-Konventionen oben
- [x] Feedback umgesetzt: Negativbeschriebe raus (Hero-Subtitle + SEO-Description); „Zur ganzen Seite" → **„Zur Erklärung"**

### Thread 3 – Flaggschiff-Seiten (schon „echt") ✅ erledigt
- [x] `next-token`: Tokens + Copy + Dark-Mode; **Temperatur-Slider** (clientseitig: `p^(1/T)` über Top-K **und** Long-Tail-Bucket, neu normiert; Zufalls-Sampling aus der temperatur-angepassten Verteilung). Light+Dark im Preview verifiziert (T 1.0→1.9→0: 88%→60%→100%)
- [x] `tokenization`: Tokens + Copy + Dark-Mode + **Multimodal-Hinweis** (Brücke zu Modell-Typen). Token-Chips (Primary) vs. ID-Chips (Secondary) im Dark-Mode unterscheidbar
- [x] `embeddings`: Tokens + Copy + Dark-Mode; **stark umstrukturiert** (4 Text-Cards → Tool zuoberst + Caption + „Mehr dazu"). **Komplett neu (Nachtrag, s. Changelog 2026-06-03): Backend OpenAI → `gemini-embedding-2` (Vertex `global`), neue „Bedeutungs-Landkarte"-Viz statt Ähnlichkeits-Heatmap.**
- [x] **Seiten-Pattern etabliert**: Header → Interaktiv (flach, `rounded-xl border bg-card`) → knappe Caption → `Accordion` „Mehr dazu" → Prev/Next. Seiten-Copy in `messages.ts` (DE+EN, `nextToken.*`/`tokenization.*`/`embeddings.*`/`common.*`)
- [x] Prev/Next aller drei Seiten auf neuen Lern-Bogen (Thread 2) ausgerichtet: Tokenisierung → Embeddings → Next-Token → Daten
- [x] `next-token` Feedback: **Beispiel-Pool** (8 Satzanfänge DE+EN, `nextToken.ex1..ex8`) – pro Seitenaufruf werden **zwei zufällig** gezeigt; der Button zeigt den ganzen Satzanfang. Würfeln im `useEffect` nach Mount (SSR/erster Render = [0,1]) → kein Hydration-Mismatch. EN im Preview verifiziert
- ℹ️ Viz-Komponenten (`next-token-prediction`, `tokenization-visualization`, `embeddings-visualization`) auf semantische Tokens umgestellt; interne DE-Strings bleiben inline (Ganz-Migration in Thread 9) – **Ausnahme: `tokenization-visualization` ist bereits vollständig i18n (DE+EN, `tokenization.viz.*`)**. Kompatibel mit Thread-2-Akzentsystem: `bg-primary`/`text-primary` erben die Akzentfarbe; grün/amber bleiben `--chart-2`/`--chart-3` (legitime Data-Viz-Ausnahme)

### Thread 4 – Training echt machen (R&D-Spike) ✅ erledigt
- [x] Prototyp: **winziges Modell live im Browser trainieren** – alle drei Ziele im Preview bestätigt (Loss 3.4→1.38, Verteilung flach→spitz `s 16%…`, Kauderwelsch→`winter/blume/himmel`)
- [x] Tech-Entscheid: **eigenes Char-Level-MLP in purem TypeScript** (kein tf.js/WASM – passt zur „echt/transparent/keine schweren Deps"-Philosophie). Im Node-Spike validiert (Backprop von Hand, ~6k Parameter, C=3/E=10/H=96)
- [x] Fake-Simulation (`training-simulation.tsx`) entfernt → neue `mini-training.tsx` (echtes Live-Training)

### Thread 5 – Finetuning / RLHF / RLVR / Reasoning ✅ erledigt
- [x] **babbage-002 aus `api/finetuning-simulate` entfernen** (Breakage-Risiko) → Route komplett auf Vertex/Gemini (OpenAI raus); Basismodell-Verhalten per Prefill-Trick reproduziert
- [x] Finetuning-Seite überarbeiten → neu im Seiten-Pattern; Basismodell vs. Assistent nebeneinander (dasselbe echte Modell, zwei Nutzungsarten)
- [x] RLHF überarbeiten → neu als echtes Belohnungsmodell-Labor (Bradley-Terry live im Browser, vom Nutzer trainiert); Reward-Hacking als Pointe, Brücke zu RLVR
- [x] **Chain-of-Thought neu** (alte gescriptete `thinking-simulation.tsx` gelöscht) → dasselbe echte Modell, zwei Modi nebeneinander (Sofort antworten vs. Schritt für Schritt), **echter JS-Prüfer fällt ✓/✗-Urteil**; Falsch→Richtig-Flip bei Mehrschritt-Arithmetik ist die Lektion (Reasoning = Rechnen, weiterhin nur Next-Token)
- [x] **NEU: RLVR** ergänzt – Reihenfolge ist **RLHF → CoT → RLVR → RAG** (User-Entscheid: CoT vor RLVR, Reasoning-Trilogie zusammenhängend). 3-Akt-Labor: echte CoT-Versuche sampeln · **echter JS-Prüfer ✓/✗ (Belohnung 1/0)** · **echtes REINFORCE** verstärkt das Geprüfte. **Umschalter Prüfer↔Eindruck** = verifizierbare Belohnung verbessert / Proxy wird gehackt → schließt den Reward-Hacking-Bogen der RLHF-Seite

### Thread 6 – RAG echt machen ✅ erledigt
- [x] Echtes Retrieval über kleinen Doku-Satz (nutzt vorhandene Embeddings-API), Antwort mit/ohne Kontext – neu im Seiten-Pattern; canned `rag-simulation.tsx` gelöscht. Live verifiziert: Retrieval-Scores, Grounding, themenfremd→ehrlicher Verzicht, Doc-Toggle, Dark/Mobile

### Thread 7 – Diffusion (NEU)
- [ ] Neue Seite/Interaktiv: Entrauschen Schritt für Schritt; Kontrast zum autoregressiven Next-Token

### Thread 8 – „KI im Einsatz" ✅ erledigt (danach auf 1 Seite verschlankt – s. Hinweis)
> **PIVOT (User-Feedback):** Die unten beschriebenen 6 Seiten wurden anschliessend auf **EINE Seite (`/privacy`)** verschlankt. Der „Wrapper anonymisiert automatisch"-Ansatz war irreführend (in der Praxis gibt's kein on-prem-Modell dazwischen); `hardware-check` & `kosten` zu volatil/technisch für die Zielgruppe; `model-types` zog nach „Hinter den Modellen" (`/multimodal`, Thread 9). Neu: ein **Nachrichten-Check** (echte sensible-Daten-Erkennung; ehrlich „so entschärfst du es selbst", kein Automatik-Wrapper) + „worauf achten" in Alltagssprache (Datenschutz/Qualität/Setup). Komponente `message-check.tsx`; alte Seiten/Komponenten gelöscht; alle alten aiInUse-Slugs → `/privacy`. (Beschrieb unten ist historisch.)
> Eigener Detailplan + Research-Anhang: [PLAN-THREAD8.md](PLAN-THREAD8.md).
- [x] **Englische Slugs + permanente 308-Redirects** (wie „Hinter den Modellen"): `/lokal-vs-cloud`→`/local-vs-cloud`, `/kosten`→`/costs`, `/datenschutz`→`/privacy`, `/werkzeugwahl`→`/choosing-tools`, `/modell-typen`→`/model-types` (alte DE-URLs leiten um, `next.config.js`). Sektions-Lern-Bogen: model-types → local-vs-cloud → hardware-check → costs → privacy → choosing-tools (`nav-items.ts` + Nav-Labels DE+EN)
- [x] `local-vs-cloud`: neu im Seiten-Pattern, **Datenfluss-Demo** (`data-flow-demo.tsx`, eine Anfrage → drei Bahnen lokal/Cloud/Wrapper). **`api/data-flow-simulate` von OpenAI → Vertex/Gemini** (Structured Output, 1 Call; erkennt sensible Stellen + anonymisiert). i18n DE+EN
- [x] `hardware-check`: **echter Speicher-Schätzer** (`hardware-checker.tsx` neu) – Params × Bytes/Gewicht (Quant) vs. Gerätespeicher, 80%-Regel; aktuelle Modelle inkl. **Apertus** (CH)
- [x] `costs`: Live-Tokenizer + **2026er-Preise** (GPT-5.x / Claude 4.x / Gemini 3.x über 3 Anbieter + DeepSeek offen), USD→CHF **0.79** (alter `/`-Wechselkurs-Bug gefixt)
- [x] `privacy`: **Datenschutz-Advisor** (`privacy-advisor.tsx`) – Sensibilität × Werkzeug → Ampel + geltendes Recht (kantonal, DSFA, EU AI Act, Gratis-vs-bezahlt-Training); Thouvenin/Volz-Quelle erhalten
- [x] `choosing-tools` (war Stub): **Synthese-Planer** (`tool-advisor.tsx`) – Prioritäten → Empfehlung lokal/Cloud/Wrapper/Abo, verlinkt die anderen Kapitel
- [x] `model-types` (war Platzhalter): **Multimodal-Tokenizer** (`multimodal-tokenizer.tsx`) – Bild → Tokens nach echten Provider-Formeln (selbes Foto, mehrfach unterschiedlich: Gemini 6'192 / OpenAI 2'500 / Claude 6'636 / Gemma 256), Audio-/Spektrogramm-Hinweis. Brücke zur Tokenisierung
- [x] OpenAI komplett raus (`openai`-Paket deinstalliert); leere Orphan-Stubs (`model-matchmaker`/`tool-planner`/`privacy-check`) gelöscht; semantische Tokens (Dark-Mode); `tsc` grün; **im Preview verifiziert** (Light/Dark, Mobile, DE/EN, alle Live-Elemente inkl. echter Vertex-Route mit `fallback:false`)

### Thread 9 – Englische Übersetzung
- [x] Alle `messages.ts`-EN-Keys füllen (wenn DE final)
- [x] `<html lang>` dynamisch / EN-Korrektheit prüfen

### Thread 10 – Politur, Accessibility, QA, Go-Live
- [x] ~~KI-im-Einsatz-Sektion entfernen~~ → **Statt nur zu löschen: Nav komplett auf 3 Sektionen entlang des Modell-Lebenszyklus umgebaut** (User-Idee). **Daten** (Trainingsdaten *(/data, umbenannt)* → Tokenisierung → Embeddings → Bild & Ton) · **Training** (Pretraining *(/training, umbenannt)* → Finetuning → RLHF → RLVR) · **Inferenz** (Next-Token → CoT → RAG → Datenschutz). Datenschutz zog in die Inferenz (technische Pointe: *wo* das Modell läuft, entscheidet was rausgeht), „KI im Einsatz" als Sektion weg. Startseite: 3 Karten statt „Zwei Wege" (Überschrift „Vom Datensatz zur Antwort"). Alle 12 Prev/Next-Ketten neu verdrahtet, nav-Labels + nextLabels DE+EN, neue Section-Keys (`nav.section.data/training/inference`), Home-Descs (`home.path.data/training/inference.desc`). URLs unverändert. `tsc` grün, im Preview verifiziert (3 Karten, alle Ketten data→…→privacy, Pretraining-Titel, keine Konsolenfehler).
- [x] **Flow-Texte zur neuen Struktur nachgezogen** (User-Feedback): Pretraining-Untertitel macht den Namen jetzt *sichtbar* (war nur im eingeklappten Accordion „…heisst Pretraining") und setzt „erste, längste Phase, alles Spätere baut darauf auf" → Brücke zu Finetuning. Next-Token-Untertitel als Inferenz-Einstieg gerahmt („Ist das Training vorbei…") statt Erst-Einführung. DE+EN.
- [x] a11y-Durchgang: globaler `:focus-visible`-Outline (Akzentfarbe) im Base-Layer → sichtbarer Tastatur-Fokus site-weit (Links hatten vorher keinen; Buttons behalten ihren Ring via Spezifität). Kontrast geprüft (muted-foreground ~5.8:1 Light / ~6.4:1 Dark, > AA). Tastatur: alle Controls sind echte Buttons; embeddings-map-Punkte sind Buttons mit aria-label; data-explorer = `role=img`+Label + „Zufälliges Dokument" als tastaturbedienbare Alternative.
- [x] Responsive/Mobile-Feinschliff: Startseite 3 Karten stapeln sauber (375px, kein Overflow); data-explorer-Umschalter `max-w-full overflow-x-auto`+`px-2.5 sm:px-3` (lief 8px über); rlvr-nextLabel „Next-Token-Prediction"→„Next-Token" (lief 3px über). Übrige Seiten overflow-frei geprüft.
- [ ] Cross-Browser & Performance (Produktions-`build` grün, 26 Seiten; Cross-Browser noch offen)
- [x] **gemini-2.5-flash → flash-lite (selektiv, je geprüft):** **rag-answer → flash-lite** (verifiziert: Grounding korrekt, Verzicht „keine Auskunft" korrekt, plain generisch). **data-flow-simulate bleibt flash** — flash-lite übersieht verlässlich nicht-offensichtliche Stellen („Schwierigkeiten zu Hause"), das ist aber der Kern der Datenschutz-Seite. **reasoning/finetuning/predict-next bleiben flash** — didaktisch fein auf flash getunt (CoT-Flip hängt an spezifischen `problems.ts`-Aufgaben; Prefill-Trick empirisch auf flash abgestimmt), Swap ohne tiefe Validierung zu riskant. flash bleibt überall als Fallback in der Kette.
- [x] **Copy-Sweep über alle Seiten:** Selbst-Beteuerungen („echt/wirklich/tatsächlich") aus sichtbaren Captions/Untertiteln raus (home-Hero, training/cot/rlvr/rlhf-Captions, data.caption, nextToken.emptyTitle, multimodal-Caption, SEO-Description …, DE+EN); Backend-Produktnamen in Captions generisch („dasselbe Sprachmodell" statt „(Gemini über Vertex AI)" bei cot/rlvr). Behalten: bedeutungstragende Vergleiche („grosse Modelle arbeiten genauso"), ehrliche Offenlegungen (Simulationsmodus-Hinweis) und die Meta-Seiten resources/impressum (dort ist das Nennen der Tools/Datenflüsse der Zweck).
- [ ] **Merge `redesign` → `main`** ← wartet auf ausdrückliche User-Freigabe

---

## Inhaltliche Ergänzungen (Einordnung)

- **RLVR** → Thread 5
- **Tokenisierung Bild/Audio** → Thread 8 (`modell-typen`), Hinweis schon in Thread 3 (`tokenization`)
- **Diffusion** → Thread 7

## Offene Punkte / Tech-Schulden

- [ ] `pnpm lint` läuft in interaktiven ESLint-Config-Prompt (Flat-Config vs. `next lint`) – Konfig reparieren
- [x] ~~`api/finetuning-simulate` nutzt **babbage-002**~~ (Thread 5) **und** ~~`data-flow-simulate` nutzt OpenAI gpt-4.1~~ (Thread 8) → **beide auf Vertex/Gemini migriert. OpenAI ist komplett aus dem Projekt** (`openai`-Paket deinstalliert, `OPENAI_API_KEY` wird nirgends mehr gebraucht). `zod` ist seither verwaist (nur noch eine Dep, harmlos)
- [ ] Preise/Modellnamen veraltet in `chain-of-thought` – Refresh. (`costs` + `model-types` in Thread 8 auf **Juni 2026** aktualisiert; aktuelle Zahlen + Quellen im Anhang von [PLAN-THREAD8.md](PLAN-THREAD8.md).)
- [ ] `public/daten/assets/{data,info}.csv` (2,1 MB) liegen weiter im **ausgelieferten** `public/`, sind aber nur noch **Build-Input** von `scripts/gen-data-sample.mjs` (kein Client-Fetch mehr). Bei Gelegenheit aus `public/` herausziehen (z. B. `scripts/data/`), damit sie nicht als statisches Asset mitdeployen.
- [ ] `<html lang="de">` statisch – bei EN-Launch dynamisch machen

## Changelog

- **2026-06-04 – Ressourcen/Impressum getrennt + redesignt:** Die alte `/resources`-Seite (noch hartcodierte Farben, kein Dark-Mode) in zwei Seiten aufgeteilt. **`/resources` neu** im Seiten-Pattern (semantische Tokens, Light/Dark verifiziert): „Über diese Webseite" mit ehrlicher Entstehungs-Einordnung (Vibe-Coding-Zitat → inzwischen Schimpfwort, Fachleute sagen „AI-Assisted Engineering"; „AI-Slop" für Designer vs. Hands-On-Wert fürs Schulfeld), Karpathy-Videos, **neue „Weitere Lernressourcen"** (3Blue1Brown, Brendan Bycroft LLM-Viz, FT-Transformer-Erklärer), FineWeb-Quelle, Thouvenin/Volz-PDF, Autor-Bio, **neue Update-Geschichte** (Zeitleiste März 2025 → Juni 2026). **`/impressum` neu** (eigene Seite, Nav-Link `nav.impressum` DE+EN): Verantwortlich (Th. Zurfluh, privates Freizeitprojekt ≠ PH Zug), **„Welche Daten werden geteilt?"** (Eingaben der Live-Übungen → Vertex/Gemini-Cloud; lokal-im-Browser-Übungen; localStorage „behind-ai-ui"; Vercel-Logs; kein Tracking), Haftung, MIT-Quellcode, Stand. Nav-Footer zeigt beide Links. Seiten-Prosa DE inline (EN-Fallback → Thread 9). `tsc` grün, Light/Dark im Preview verifiziert.
- **2026-06-03 – Thread 1:** Fundament & Shell. Branch `redesign` angelegt.
- **2026-06-03 – Cleanup:** `.app.mdx` (Vibecoding-Artefakt) und ungenutzte create-next-app-SVGs (`file/globe/next/vercel/window.svg`) entfernt.
- **2026-06-03 – Thread 2:** Startseite & Informationsarchitektur. Neue Home (Hero + echte Next-Token-Mini-Demo + Zwei-Pfade-Karten aus `navSections` + optionales „Mehr dazu"-Accordion), semantische Tokens, DE+EN-Copy (`home.*`). Lern-Bogen finalisiert (Embeddings nach vorn). Neue Komponente `next-token-mini.tsx`.
- **2026-06-03 – Thread 3:** Flaggschiff-Seiten `next-token`, `tokenization`, `embeddings` neu im Seiten-Pattern (Interaktiv zuoberst, flach, Caption, „Mehr dazu"-Accordion), semantische Tokens (Light+Dark im Preview verifiziert), DE+EN-Copy. **Next-Token: Temperatur-Slider** (clientseitige Umformung der Verteilung + temperaturbasiertes Sampling). **Tokenisierung: Multimodal-Hinweis.** **Embeddings: Tool nach vorn** statt 4 Text-Cards. Prev/Next auf den Thread-2-Lern-Bogen ausgerichtet. Viz-Komponenten dark-mode-fähig (interne Strings → Thread 9). `pnpm exec tsc --noEmit` grün.
- **2026-06-03 – Thread 3 (Nachtrag, User-Feedback „Tokenisierung zu voll"):** Viz `animations/tokenization-animation.tsx` → `visualizations/tokenization-visualization.tsx` (`TokenizationVisualization`) verschoben/umbenannt → einheitlich mit den übrigen Visualisierungen; `animations/`-Ordner entfernt. **Vereinfacht:** redundante Doppelung („So sieht das Modell den Text" + „Tokens") zu **einer** Token-Ansicht zusammengeführt, **Token-IDs** behalten, **rekonstruierten Text**, die zweite BPE-Erklärbox, den Original-Block und die Kontextfenster-Box entfernt. Mehrstufige Timer-Animation raus → schlanke gestufte CSS-Einblendung (Tokens → IDs). Framer-Motion-Eintrittsanimation entfernt (war rAF-abhängig → im Hintergrund-Tab/Reduced-Motion unsichtbar); Ruhezustand jetzt garantiert sichtbar. Whitespace in Token-Chips sichtbar (führendes Leerzeichen → `·`). Hover-Linking Token↔ID erhalten. **Interne Strings vollständig nach i18n migriert (DE+EN, `tokenization.viz.*`)** – die Komponente ist damit zweisprachig (Zähl-Zeile aus Wort-Teilen komponiert, da `t()` keine Interpolation kann). Light/Dark **und DE/EN** im Preview verifiziert (Token-Chips Akzent vs. ID-Chips neutral unterscheidbar; Labels wechseln korrekt mit der Sprache), `tsc` grün.
- **2026-06-03 – Thread 3 (Nachtrag, `embeddings` komplett neu):** **Backend OpenAI `text-embedding-3-small` → Google `gemini-embedding-2`** über Vertex AI. Wichtig: gemini-embedding-2 ist (in diesem Projekt) nur über die Vertex-Region **`global`** + `…:embedContent` erreichbar (regionale Endpunkte wie `us-central1` → 404; `:predict` im global → 404). Auth = dasselbe Dienstkonto wie Next-Token (kein neuer API-Key). `output_dimensionality:768`. `api/embeddings/route.ts` neu (Bare-Response `{ embedding, model, dim }`); `api/embeddings/terms` + `public/embeddings.json` (2,1 MB Roget-Begriffe) **entfernt**. **Neue Viz „Bedeutungs-Landkarte"** (`visualizations/embeddings-map.tsx`) statt Ähnlichkeits-Heatmap: ~80 deutsche Alltagswörter in 5 Kategorien (chart-1..5), platziert per **SMACOF/metrischem MDS** über echte Cosinus-Distanzen (Bildschirm-Distanz ≈ Bedeutungs-Distanz; Cluster-Trennung zwischen/innerhalb ≈ 2×). **Token-Brücke:** Wort/kurzer Satz eingeben → jedes Wort wird **live** embedded und per ähnlichkeitsgewichtetem Nachbar-Schwerpunkt platziert (fällt zu seinen Nachbarn); Klick/Tap auf einen Punkt → **Nachbar-Panel** mit echter Cosinus-Ähnlichkeit (Balken) + Linien zu Top-3. Akzentfarbe = Nutzer-Wörter. **Mobile:** nur farbige Cluster, Labels beim Tap (kein Hover). Daten via Build-Script `scripts/gen-embeddings-map.mjs` → `public/embeddings-map.json` (448 KB, gerundete Vektoren). **Modell + 768-Dim müssen zwischen Route und Script identisch bleiben** (sonst liegt das Live-Wort in einem anderen Raum als die Karte). Wort-Arithmetik (König−Mann+Frau→Königin ✓, aber Hauptstadt-Analogien unzuverlässig) verworfen. Interne Viz-Strings bleiben DE (Konvention → Thread 9). Light/Dark/Mobile im Preview verifiziert, `tsc` grün.
- **2026-06-03 – `embeddings` Iteration 2 (User-Feedback):** (1) **8 Kategorien à 18 Wörter** (144 statt 80) – neu: Sport, Musik, Fahrzeuge; dafür `--chart-6/7/8` (Teal/Lime/Rot) in globals.css ergänzt (Light+Dark). (2) **Mehr Spread:** Layout-Normalisierung im Script von uniformer Max-Radius- auf **per-Achse-Perzentil-Streckung** (5–95 % → ±0.92) → Karte nutzt die ganze Fläche. (3) **Single-Embedding statt Satz-Zerlegung:** Eingabe (Wort *oder* Satz) wird als EIN Vektor gerechnet; Scaffolding „Wort eingeben", `maxLength 30`. (4) **Kategorie-Filter:** Legende ist klickbar → Kategorie aus-/einblenden (durchgestrichen/ausgegraut; gefiltert in Render + Nachbar-Pool) für Übersichtlichkeit. Außerdem: Label-Lesbarkeit von Hintergrund-Chip auf **Text-Halo** (`textShadow` in `--background`) umgestellt (im Dark-Mode kein graues Kästchen-Rauschen mehr); langes Wort-Label gekürzt. Light/Dark/Mobile + Toggle + Live-Embedding im Preview verifiziert, `tsc` grün.
- **2026-06-03 – `data` komplett neu (FineWeb-Scatter war „zu kompliziert"):** Plotly-Punktwolke + 3 Text-Cards ersetzt durch **erkundbare Karte echter FineWeb-Dokumente** im Seiten-Pattern (Interaktiv zuoberst, Caption, „Mehr dazu", DE+EN-Copy `data.*`). **Lernziel (mit User geschärft):** Pretraining-Daten sind extrem heterogen & thematisch *ungeplant* (kein Lehrplan), aber qualitativ *hart gefiltert* – und genau diese Auswahl ist der Hebel. Reine Themen-Karte würde Daten „ordentlicher" aussehen lassen als sie sind → deshalb echte Doku-Texte beim Klick + sichtbarer Filter. **Neue Viz `visualizations/data-explorer.tsx`:** 953 echte (übersetzte) Dokumente als **SVG-Punkte** (perf, kein framer-motion pro Punkt), Layout aus den **echten UMAP-Koordinaten** der CSV (nicht neu gerechnet), 104 Cluster → **8 Themengruppen** (chart-1..8). **Dreifach-Umschalter:** *Roh* (alle, Punkt klicken → echtes Dokument lesen) · *Deine Auswahl* (was du mit Behalten/Raus behalten hast) · *Musterlösung* (was der echte FineWeb-Edu-Filter behält, Score ≥3 = **58/953 ≈ 6 %**). Beim Wechsel verblassen inaktive Punkte (klein + `fill-opacity 0.06`) → die ~94 % Reduktion wird **sichtbar** (wichtig: inaktive müssen klein *und* blass sein, sonst stapelt sich Transparenz in dichten Clustern wieder zu Vollfarbe). Beurteilen ist **optional** (Explorieren ohne Zwang); nach Entscheid Aufdeckung: ★-Score + Filter-Urteil + einig/uneinig. Legende blendet Gruppen aus. **Daten via `scripts/gen-data-sample.mjs` → `public/data-sample.json` (328 KB):** kuratiert jugendfrei (Deny-Cluster Glücksspiel/Cannabis/Sucht/Waffen/Sexualität…, rote-Begriffe-Filter, Text auf ~340 Zeichen gekürzt), Koordinaten 0..1 über die behaltenen Punkte normiert. **Deps `plotly.js-basic-dist-min` + `react-plotly.js` + `papaparse` (+ @types) entfernt**, `datenset-cluster.tsx` + Plotly-Typdecl gelöscht. Interne Viz-Strings DE (Konvention → Thread 9). Light/Dark/Mobile + DE/EN + alle 3 Ansichten im Preview verifiziert, `tsc` grün.
- **2026-06-03 – Thread 2 (Nachtrag, User-Feedback):** **Akzentfarben-Picker** im Header (`accents.ts` + `accent-provider.tsx` + `accent-toggle.tsx`, 16 Tailwind-Farben, persistiert, Light/Dark getrennt), **Default auf „Sky"** (globals.css + Store). Copy-Regel „keine Negativbeschriebe" ergänzt; „statt langer Texte" aus Hero & SEO-Description entfernt; Demo-Link „Zur ganzen Seite" → „Zur Erklärung". Im Preview verifiziert (Picker, Persistenz, Light/Dark), `tsc` grün.
- **2026-06-03 – Thread 4 (`training` echt gemacht):** Fake-Simulation (`training-simulation.tsx`, gescriptete Wahrscheinlichkeiten) ersetzt durch **echtes Live-Training eines winzigen Sprachmodells im Browser**. Seite neu im Seiten-Pattern (Interaktiv zuoberst, Caption nennt „echtes neuronales Netz", „Mehr dazu"-Accordion, Prev/Next /data→/finetuning, DE+EN-Copy `training.*`). **Tech-Entscheid: eigenes Char-Level-MLP in purem TypeScript** (kein tf.js/WASM – passt zu „echt/transparent/keine schweren Deps"). Architektur (Bengio/Karpathy makemore): Kontext C=3 Zeichen → Embedding E=10 → tanh-Hidden H=96 → Softmax übers Vokabular; **Backprop von Hand**, SGD, ~6000 Parameter, Float32. Im **Node-Spike** (`/tmp`, nicht im Repo) validiert: Loss 3.4→~1.2, saubere Eval-Kurve, plausible Wörter/Namen, ~37k Beispiele/s. **Drei synchrone Live-Ansichten = die drei Lernziele:** Loss-Kurve (SVG, fällt, gestrichelte „Rateniveau"-Linie bei log V), Textproben (Kauderwelsch→echte Wörter), **Verteilung fürs nächste Zeichen** (flach→spitz; bewusst dieselbe Balken-Bildsprache wie die Next-Token-Seite → starke Brücke). **Korpus-Umschalter Wörter/Namen** macht die Daten-Botschaft erfahrbar (gleiche Mechanik, andere Daten → andere „Sprache"). Tempo Normal (~15 s, zuschaubar) / Turbo (~2 s). Module: `src/lib/mini-llm/{model,trainer,corpora}.ts` (rein, framework-frei, transparent kommentiert; kuratierte jugendfreie DE-Korpora inline ~KB), Komponente `visualizations/mini-training.tsx` (Modell in Refs, Training über `requestAnimationFrame` mit Schritt-Budget/Frame, nur gedrosselte Snapshots in den State; Pre-Mount-Platzhalter gegen Hydration-Mismatch). Interne Viz-Strings DE (Konvention → Thread 9). **Verifiziert** im Preview: Loss fällt 3.4→1.38, Verteilung flach→`s 16% w 11%…` (typische dt. Wortanfänge), Proben `winter/blume/himmel/liege`, Korpus-Wechsel→Namen `ursula/pia/romy/jan`; Light/Dark, Mobile/Desktop, DE/EN. `tsc` grün. **Hinweis (rAF-Eigenheit):** in der headless-Preview feuert `requestAnimationFrame` nur bei aktivem Repaint (Screenshot/Interaktion), nicht bei passivem Warten → Training-Fortschritt im Test nur über Screenshots messbar; bei sichtbarem Tab (realer Nutzer) läuft rAF mit 60 fps normal.
- **2026-06-03 – Thread 4 (Nachtrag, User-Feedback):** (1) **Lerndaten einsehbar** – „Lerndaten ansehen" blendet die echten Korpus-Wörter ein („das Modell hat nur diese N Wörter gesehen"). (2) **Offenes Training statt fixem 5000-Limit:** Play läuft endlos weiter; neue **„+50"/„+500"-Schritt-Buttons** (landen exakt, dank `min(spf, remaining)`) zum kleinschrittigen Weitertrainieren; Fortschrittsbalken zeigt jetzt „Gelerntheit" (Loss-Abstand zum Rateniveau) statt Schrittquote; Loss-Kurve skaliert x automatisch mit. (3) **Drittes Tempo „Zeitlupe"** (1 Schritt/Frame) neben Normal/Turbo. (4) **Novel-Marker:** generierte Wörter, die NICHT in den Lerndaten stehen, werden blau markiert (Legende „selbst zusammengesetzt") → zeigt Memorieren vs. Generalisieren (im Preview: bei Schritt 1200 `sein`/`suchen` neutral=echt neben `res`/`brat`/`früs` blau=erfunden). (5) Grammatik-Fix Loss-Caption („läge der Fehler bei …" statt mehrdeutigem „läge er"). (6) „Kein Video"-Negativbeschrieb der Seiten-Caption positiv umformuliert (Copy-Regel). `tsc` grün, im Preview verifiziert (offenes Training, +N exakt, Novel-Mix, Lerndaten-Panel, Light/Dark/DE-EN).
- **2026-06-03 – Thread 5 (`finetuning` neu + babbage-002 raus):** **babbage-002-Breakage beseitigt:** `api/finetuning-simulate/route.ts` komplett von OpenAI (babbage-002 / gpt-4.1-mini) auf **Vertex/Gemini** umgestellt – dasselbe Dienstkonto wie Next-Token/Embeddings (kein OpenAI-Key mehr; `OPENAI_API_KEY` nur noch für `data-flow-simulate`). **Kernidee (User):** „Pretraining auch einfach prompten" – ein echtes Basismodell gibt's per API kaum, also wird Basismodell-Verhalten per **Prefill-Trick** reproduziert: Eingabe als `model`-Turn + Anweisung „setze als rohes Sprachmodell bloss fort, antworte NICHT" → Gemini driftet zuverlässig in FAQ-/Fragenlisten ab statt zu antworten (genau die berühmte Base-Model-Fehlfunktion). **Empirisch derisked:** Prefill ohne Prime endete oft sofort (`finishReason=STOP` → leere Fortsetzung, ~25–80 % je Query, korreliert nicht unabhängig); Fix = **`\n\n`-Prime am Prefill-Ende** (gibt „Schwung", 4–5/5 volle Fortsetzungen) **+ Retry-on-empty/zu-kurz** (`minLen` base 40 / assistant 1, bis zu 3 Versuche). Modell-Fallback-Kette + thinkingBudget 0 wie Next-Token. Jugendfrei-Klausel im Basis-Prompt (roher Webtext streifte sonst heikle Themen). Assistent = normaler Chat + System-Prompt (Spiegelstriche statt Markdown, Cleanup in der Komponente). **Seite neu im Seiten-Pattern** (`finetuning/page.tsx`, DE+EN-Copy `finetuning.*`, Prev /training → Next /rlhf): **eine Eingabe → zwei Panels nebeneinander** (Basismodell „nur Pretraining" vs. Assistent „nach Finetuning", Akzent am Assistenten). Neue Komponente `visualizations/finetuning-comparison.tsx` (zwei unabhängige parallele Fetches, höhengedeckelte Panels mit Scroll, Per-Panel-Erklärnotiz, Auto-Run der Standardfrage beim Mount). **Alte `finetuning-simulation.tsx` gelöscht.** Interne Viz-Strings DE inline (Konvention → Thread 9). `tsc` grün; im Preview verifiziert (Marie-Curie: Basis → Fragenliste, Assistent → Antwort; Gedicht: Basis ignoriert Anweisung, Assistent dichtet; Light/Dark, Mobile-Stack, keine Konsolenfehler). **Feedback-Runde:** maxTokens getrennt (Basis 100 = bricht sichtbar ab „stoppt nicht von selbst" / Assistent 256 = hört von selbst auf); Caption-Mechanismus-Reveal entfernt; Vermenschlichung raus („brillant"→„treffsicher", „weiss nicht"→„nicht darauf ausgelegt"); Markdown-Cleanup auch im Basis-Panel.
- **2026-06-03 – Thread 4 (Nachtrag 2, User-Feedback – 4 Vertiefungen + 4 Fixes):** **Fixes:** (a) **Bug:** Schrittanzeige blieb bei 5000 stehen – `Trainer.runSteps` hatte noch das alte `!this.done`-Limit; entfernt, LR-Decay jetzt auf **absolute Schritte** (>2000→×0.5, >4000→×0.25) statt fixem Ziel → echtes offenes Training. (b) **Tempo gespreizt** via Akkumulator (Bruch-Schritte/Frame): Zeitlupe 0.25 / Normal 6 / Turbo 60 (vorher 1/6/40, kaum Unterschied); „+N" läuft tempo-unabhängig zügig (INCREMENT_SPF=6). (c) Nav-Label **„Training vs. Inferenz" → „Training"** (`nav.training` + `data.nextLabel`, DE+EN). (d) **„überrascht" entschärft** (zu anthropomorph) – Loss-Caption + `training.moreP2` umformuliert (DE+EN). **Vertiefungen (in `mini-llm/{model,trainer,corpora}.ts` + `mini-training.tsx`):** (1) **Eigene Lerndaten** – drittes Korpus „Eigene" mit Textarea; `buildCustomCorpus()` baut aus Nutzertext einen sauberen Korpus (Default: 40 Früchte) → stärkster „Daten formen das Modell"-Moment (Brücke zur Daten-Seite). (2) **Zeichen-Embeddings** – `Trainer.embedding2D()` (PCA von Hand: Power-Iteration + Deflation auf der E×E-Kovarianz, Vorzeichen stabilisiert) projiziert die gelernten Char-Embeddings live in 2D; Vokale grün (chart-2), ordnen sich beim Training → Brücke zur Embeddings-Seite. (3) **Temperatur auf die Proben** – Slider 0.3–1.5 (brav↔wild), `sample(…, T)` → Brücke zur Next-Token-Seite. (4) **Trainingsbeispiel** – `probeExample()` + `deriveExample()` (sucht den reinsten 3-Gramm→Zeichen-Fall im Korpus) zeigt „aus ctx soll target werden" + live steigenden p(richtig)-Balken + ✓. (2)+(4) hinter einer **„Mehr Einblicke – für Interessierte"**-Disclosure (Default schlank). `tsc` grün; im Preview verifiziert: offenes Training (60→300→420, kein 5000-Stopp), Embeddings (29 Zeichen, Vokale clustern), Beispiel `che→n ✓ 49%`, Eigene-Tab (40 Früchte, Reset auf 0), Temperatur, Dark/Mobile.
- **2026-06-03 – Thread 6 (`rag` echt gemacht):** Alte canned Simulation (`rag-simulation.tsx`: hartcodierte `vanillaAnswer`/`ragAnswer` pro Szenario + Fake-Fortschrittsbalken, hartcodierte Farben) ersetzt durch **echte Retrieval-Augmented-Generation-Pipeline**. **Kernidee:** das *Echte* an RAG ist die Ähnlichkeitssuche (= die Embeddings-Seite) + Grounding – beides live machbar mit dem bereits gebauten Stack. **Wissensbasis = erfundene Schule** (Lindenhof-Schule, 14 kurze jugendfreie Dokumente): *fiktiv ist die ehrliche Wahl* – garantiert, dass das Modell die Fakten nie aus dem Training kennt → sauberer Kontrast „ohne/mit Kontext"; genau der reale RAG-Fall (internes Wiki). **Pipeline (alles echt):** (1) **Abrufen** – Frage live über `/api/embeddings` (gemini-embedding-2) eingebettet, **Cosinus-Ähnlichkeit clientseitig** (`src/lib/rag/retrieval.ts`, `unit`+`dot`+`rankDocs`, identische Mathe wie embeddings-map) gegen alle Docs → Top-3 mit **sichtbaren Werten/Balken**; (2) **Anreichern** – die Top-3 vor die Frage gestellt; (3) **Antworten** – neue Route `/api/rag-answer` (Vertex/Gemini, gleiches Dienstkonto/Fallback-Kette wie finetuning-simulate) liefert **zwei parallele Antworten**: *ohne Kontext* (normaler Assistent) vs. *mit Kontext* (striktes Grounding: „antworte NUR aus den Unterlagen, sonst sag offen, dass nichts dazu drinsteht"). **Pointe = „RAG ist nur so gut wie das, was es findet":** Balken-Skala absolut → themenfremde Frage zeigt durchgehend kurze Balken; Doc-Toggle (Auge) nimmt Dokumente aus der Wissensbasis → live neu geranked + „Wissensbasis geändert"-Hinweis. **Build-Script `scripts/gen-rag-docs.mjs` → `public/rag-docs.json` (81 KB):** embeddet Titel+Text der 14 Docs (Modell+768-Dim **identisch** zur Live-Route, sonst anderer Vektorraum), mit Stichproben-QC. **Wichtiger Retrieval-Fund:** Frage **ohne** Schulnamen formulieren – sonst dominiert das generische „Über die Schule"-Doc (Top-1 für fast alles); ohne Namen treibt das Thema die Suche (Top-1 je korrekt: leitung/bibliothek/projektwoche/mensa/musikzimmer…, themenfremd ~0.51 flach). Beispiel-Chips + Default daher ohne Namen („Wer leitet die Schule?"), der Bezug steht in der Wissensbasis. **Seite neu im Seiten-Pattern** (`rag/page.tsx`, DE+EN `rag.*`, Prev /chain-of-thought → Next /lokal-vs-cloud „KI im Einsatz"; RAG ist Schluss des Lern-Bogens). **Alte `rag-simulation.tsx` gelöscht.** Interne Viz-Strings DE inline (Konvention → Thread 9). `tsc` grün; im Preview verifiziert: „Wer leitet die Schule?" → ohne Kontext generisches Geschwafel, mit Kontext „Rektorin Dr. Astrid Velm seit 2019" (geerdet auf Schulleitung/Sekretariat/Über die Schule); Fussball-WM-2022 → ohne Kontext „Argentinien", mit Kontext „Unterlagen geben keine Auskunft"; Bibliothek-Doc ausgeblendet → Antwort kippt korrekt zu „keine Auskunft" + Stale-Hinweis; Light/Dark, Mobile-Stack, DE, keine Konsolenfehler.
- **2026-06-03 – Thread 5 (`rlhf` komplett neu gedacht):** Alte gescriptete 6-Phasen-Simulation (`rlhf-simulation.tsx`, Daumen-hoch-Quiz + Fake-Fortschrittsbalken) ersetzt durch **echtes Belohnungsmodell-Labor**. **Kernidee:** das *Echte* an RLHF ist das Belohnungsmodell – und es ist live im Browser machbar. **Reale Bradley-Terry-Logistik** (`src/lib/rlhf/reward-model.ts`, `reward(x)=w·merkmale`, Loss `−log σ(w·Δ)`, SGD von Hand + L2) auf den echten Klicks des Nutzers – genau das InstructGPT-Reward-Objektiv, nur mit **5 ablesbaren Stil-Merkmalen** (freundlich/strukturiert/ausführlich/selbstsicher/schmeichelhaft) statt Transformer-Trunk. **4 Stationen, ein Faden:** (1) **Bewerten** – Nutzer wählt bei 6 Antwort-Paaren die bessere (echte Vergleiche); (2) **Lernen** – RM trainiert live (Loss fällt + gelernte **signierte Gewichte** als Balken = „dein Geschmack als Zahlen"); (3) **Bewähren** – RM trifft bei 3 ungesehenen Paaren deine Wahl (Verallgemeinerung, im Test 3/3 aus 6 Klicks); (4) **Überlisten** – Reward-Scatter (Belohnung x vs. **versteckte** Hilfe y): das „Sprachmodell" wählt die Antwort mit der höchsten Belohnung = ein **Blender** (stilistisch top, faktisch falsch – „Hauptstadt ist Sydney/leere Schmeichelei" statt Canberra), liegt unten/rechts → max Reward ≠ max Hilfe = **Reward Hacking**. **Versteckte Wahrheit nicht im RM-Input** → Hacking entsteht *ehrlich* (das Modell sieht nur Stil, nicht die Wahrheit); Brücke zu RLVR/Reasoning („Belohnung nicht raten, sondern prüfen"). **Vor dem UI-Bau im Node-Spike derisked** (`/tmp`, nicht im Repo): BT-SGD konvergiert aus 6–20 Klicks, Blender gewinnt zuverlässig, Over-Optimization-Kurve (Gao et al.) reproduziert. Module `src/lib/rlhf/{reward-model,data}.ts` (rein, framework-frei, DE-kommentiert; kuratierte jugendfreie DE-Antworten inline), Komponente `visualizations/rlhf-lab.tsx`, Seite `app/rlhf/page.tsx` neu im Seiten-Pattern (DE+EN `rlhf.*`, Prev /finetuning → **Next /chain-of-thought** statt altem falschen /rag). **Training-Abspielen per `setInterval` statt rAF** (läuft auch in inaktiven Tabs garantiert zu Ende → „Weiter"-Knopf erscheint zuverlässig; Trajektorie synchron vorberechnet). Alte `rlhf-simulation.tsx` gelöscht. Interne Viz-Strings DE inline (Konvention → Thread 9). `tsc` grün; im Preview verifiziert: Gewichte exakt wie Spike (+1.6/+2.6/+1.3/+0.4/+0.2), Loss-Kurve, Verallgemeinern 3/3, Reward-Hacking-Scatter + Reveal („gewählt"=Blender, Canberra-Auflösung), Light/Dark, DE/EN (Seiten-Copy übersetzt, Viz-Interna DE), Mobile-Stack, keine Konsolenfehler.
- **2026-06-03 – Thread 5 (`chain-of-thought` + `rlvr` komplett neu, zusammen gedacht):** Beide Reasoning-Seiten neu. **User-Entscheid: CoT *vor* RLVR** → Lern-Bogen jetzt `… RLHF → Chain-of-Thought → RLVR → RAG` (Reasoning-Trilogie zusammenhängend; `nav-items.ts` + Prev/Next). **Vorab gegen das echte gemini-2.5-flash derisked** (zwei Wegwerf-Spikes, `/tmp`): klare didaktische Aufteilung fiel aus den Daten – **Mehrschritt-Arithmetik** für CoT (Sofort-Antwort konsistent FALSCH 7/8 Ausdrücke 4/4, Schritt-für-Schritt RICHTIG 4/4), **Buchstaben-Zählen** für RLVR (selbst mit Versuchen STREUUNG → echter Mix; Arithmetik unter CoT zu zuverlässig → keine Streuung). RL-Spielzeug (REINFORCE) bestätigt: Prüfer→richtig 97 %, Stil-Proxy→Blender 91 %.
  • **CoT** (`chain-of-thought/page.tsx` neu im Seiten-Pattern; alte gescriptete `thinking-simulation.tsx` **gelöscht**): Komponente `visualizations/cot-comparison.tsx` = zwei parallele Fetches, **„Sofort antworten" vs. „Schritt für Schritt"** (dasselbe Modell), **echter JS-Prüfer** (`evalArith`) fällt **✓/✗-Urteil** (das Finetuning-Panel nicht hat → klar unterschieden). Auto-Run + Aufgaben-Chips + freie Eingabe. Lektion: Mitdenken = weitere vorhergesagte Tokens als eigener Kontext, weiterhin nur Next-Token.
  • **RLVR** (`rlvr/page.tsx` **neu**, URL `/rlvr`): Komponente `visualizations/rlvr-lab.tsx` = **3-Akt-Labor** (an `rlhf-lab` angelehnt: Stationsleiste, Balken/Kurve, Trajektorie-vorberechnen-dann-per-`setInterval`-abspielen). (1) **Generieren** – N=8 echte Stegreif-Versuche (Gemini @ Temp) widersprechen sich; (2) **Prüfen** – **echter JS-Prüfer** (`countLetter`, Wort mit hervorgehobenen Treffern) markiert ✓/✗ → Belohnung 1/0; (3) **Verstärken** – **echtes REINFORCE** über die Antwort-Klassen mit der geprüften Belohnung. **Umschalter Prüfer↔Eindruck:** Prüfer → konzentriert auf die richtige Antwort (Treffer ↑); Eindruck (Popularitäts-Proxy) → konzentriert auf die häufigste/überzeugendste FALSCHE → **Reward Hacking** sichtbar → schließt den RLHF-Bogen. Drei Ausgänge ehrlich behandelt (Mehrheit-falsch=Hack / Mehrheit-richtig=„Crowd zufällig richtig" / alle-falsch=„RL verstärkt nur, was vorkommt").
  • **Technik:** Route `api/reasoning/route.ts` (Vertex/Gemini wie predict-next/finetuning, `thinkingBudget:0`, 3 Modi `direct`/`cot`/`sample`). Lib `src/lib/reasoning/{verify,problems,reinforce}.ts` (rein, framework-frei, DE-kommentiert). **`sample`-Modus = Schätzung aus dem Stegreif** (ohne pedantisches Durchzählen) – bringt die *echte* Tokenisierungs-Schwäche zum Vorschein und erzeugt erst die Streuung (methodisches CoT trivialisiert die Aufgabe → keine Streuung; Brücke zur CoT-Seite). Default-Wort **Schifffahrtsgesellschaft/f** (3-Wert-Spread 4/5/6, „fff"-Hook, verlässlich Mehrheit-falsch mit korrektem Versuch). `messages.ts` `cot.*`/`rlvr.*`/`nav.rlvr` (DE+EN); `rag`-Prev-Link → `/rlvr`. Interne Viz-Strings DE (Konvention → Thread 9).
  • **Verifiziert** (`tsc` grün): CoT-Flip live (Sofort 19/37/61 ✗ → Schritt-für-Schritt −31 ✓) in Light/Dark/Mobile/DE/EN; RLVR Generieren-Streuung, Prüfen ✓/✗ + „2 von 8" + Wort-Highlight + alle-falsch-Fall, Verstärken Prüfer→96 % auf richtig (4-Balken-Spread + steigende Kurve), Eindruck→konzentriert auf falsche Mehrheit (Hack); keine Konsolenfehler. **Headless-Preview-Gotcha:** der `setInterval` der Verstärken-Animation wird in der Headless-Preview gedrosselt und erreicht `play===1` nicht ganz → das `done`-Banner ist dort nicht sichtbar (im echten Browser läuft es zu Ende, wie das identische RLHF-Labor-Pattern).
